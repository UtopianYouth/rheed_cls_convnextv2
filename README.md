ConvNeXt V2 复现

1. Mixup 数据增强
定义
Mixup是一种正则化技术，通过线性插值创建虚拟训练样本。

2. EMA（指数移动平均）
定义
EMA是一种模型权重的平滑技术，用于获得更稳定的模型。

3. AMP（自动混合精度）
定义
AMP是自动混合精度训练，通过使用FP16和FP32混合计算来加速训练。

4. MAE（掩码自编码器）训练
定义
MAE是一种自监督学习方法，通过随机掩码输入并重建完整数据。